framework:
    rate_limiter:
        registration:
            policy: 'fixed_window'
            limit: 3
            interval: '1 hour'
        password_change:
            policy: 'sliding_window'
            limit: 5
            interval: '1 hour'
        update_user:
            policy: 'sliding_window'
            limit: 10
            interval: '1 hour'
        anilist_search_burst:
            policy: 'sliding_window'
            limit: 15
            interval: '1 minute'
        anilist_detail_burst:
            policy: 'sliding_window'
            limit: 20
            interval: '1 minute'
        anilist_search_hour:
            policy: 'sliding_window'
            limit: 80
            interval: '1 hour'
        anilist_detail_hour:
            policy: 'sliding_window'
            limit: 120
            interval: '1 hour'
        anilist_import:
            policy: 'fixed_window'
            limit: 5
            interval: '1 day'
